[pytest]
# Put the project folder on the import path, so the PDF tests can import the
# pipeline package however pytest is started. The PowerPoint tests add it
# themselves; this saves the PDF tests from having to.
pythonpath = .
